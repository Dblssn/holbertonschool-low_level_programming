#include <stdio.h>
/**
* main - check if the code prints the alphabet in lowercase
*
* Return: Always 0
*/
int main(void)
{
char c;

for (c = 'a'; c <= 'z'; c++)
{
putchar(c);
}
putchar('\n');

return (0);
}
